{
  "name": "@vsa/plugin",
  "version": "0.1.5",
  "private": true,
  "description": "VaultSync for Agents Obsidian plugin (desktop + mobile): pairing UI, live sync, status indicator, obsidian:// deep links",
  "license": "MIT",
  "type": "module",
  "main": "./src/main.ts",
  "types": "./src/main.ts",
  "exports": {
    ".": "./src/main.ts"
  },
  "scripts": {
    "build": "node esbuild.mjs",
    "build:production": "node esbuild.mjs --production",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@noble/hashes": "^2.3.0",
    "@vsa/core": "^0.1.0",
    "fflate": "^0.8.3"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "obsidian": "^1.13.1",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
