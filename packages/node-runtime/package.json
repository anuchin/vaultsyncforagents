{
  "name": "@vsa/node-runtime",
  "version": "0.1.0",
  "private": true,
  "description": "VaultSync for Agents Node runtime glue: fs storage adapter, HTTP blob store, WebSocket transport, machine config (shared by CLI and daemon)",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@vsa/core": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0"
  }
}
