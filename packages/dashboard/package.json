{
  "name": "@vsa/dashboard",
  "version": "0.1.3",
  "private": true,
  "description": "VaultSync for Agents admin dashboard SPA (claim + status), built to dist/ and served by the worker's static assets binding",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.6",
    "typescript": "^5.9.3",
    "vite": "^7.3.6",
    "vitest": "^3.2.4"
  }
}
